# FILL.AI Frontend - React Version

Современный веб-интерфейс для платформы AI-генерации курсов FILL.AI, построенный на React + Vite + Tailwind CSS.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
cd frontend_service
npm install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:3000`

### 🎮 Демо-режим (БЕЗ BACKEND)

**Демо-режим включен по умолчанию!** Это означает:

- ✅ Автоматический вход при первом запуске
- ✅ Все данные мокируются (6 готовых курсов)
- ✅ Граф знаний работает из коробки
- ✅ Генерация новых курсов работает
- ✅ Не требуется backend сервер

**Просто запустите `npm run dev` и откройте браузер!**

При первом открытии вы автоматически войдете в систему и попадете на Dashboard с графом знаний.

Подробнее: [DEMO.md](DEMO.md)

### Сборка для продакшена

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 📁 Структура проекта

```
frontend_service/
├── src/
│   ├── components/          # React компоненты
│   │   ├── Header.jsx      # Шапка приложения
│   │   ├── Sidebar.jsx      # Боковая панель навигации
│   │   ├── GraphView.jsx    # Граф знаний (vis-network)
│   │   ├── PopupPanel.jsx   # Всплывающая панель справа
│   │   ├── SearchBar.jsx    # Поиск и генерация курсов
│   │   └── CourseModal.jsx  # Модальное окно курса
│   ├── pages/              # Страницы приложения
│   │   ├── LandingPage.jsx  # Лендинг
│   │   ├── LoginPage.jsx    # Вход
│   │   ├── RegisterPage.jsx # Регистрация
│   │   ├── DashboardPage.jsx # Главная панель с графом
│   │   ├── ProfilePage.jsx  # Профиль пользователя
│   │   ├── CommunityPage.jsx # Сообщество
│   │   ├── FAQPage.jsx      # FAQ
│   │   └── AboutPage.jsx    # О проекте
│   ├── contexts/           # React Context
│   │   └── AuthContext.jsx # Контекст авторизации
│   ├── services/           # Сервисы
│   │   ├── apiService.js    # API клиент
│   │   └── mockService.js   # Моки для демо-режима
│   ├── App.jsx             # Главный компонент
│   ├── main.jsx            # Точка входа
│   └── index.css           # Глобальные стили (Tailwind)
├── package.json
├── vite.config.js          # Конфигурация Vite
├── tailwind.config.js      # Конфигурация Tailwind
└── postcss.config.js       # Конфигурация PostCSS
```

## 🎨 Дизайн

Интерфейс построен на минималистичной темной теме:

- **Цвета**: Черно-белая палитра с акцентами
- **Типографика**: Inter font family
- **Стиль**: Минималистичный, современный
- **Граф знаний**: Центральный элемент интерфейса

## 🔑 Ключевые особенности

### Dashboard
- **Граф знаний в центре**: Всегда видимый, интерактивный граф с узлами курсов
- **Sidebar слева**: Компактная навигация с разделами
- **Popup панель**: Появляется справа при клике на узел графа
- **Search Bar**: Генерация новых курсов через поиск

### Граф знаний
- Использует `vis-network` для визуализации
- Центральный узел - пользователь
- Узлы первого уровня - основные направления (RU, EN, 🌐)
- Узлы второго уровня - курсы и темы
- Интерактивность: клик на узел открывает popup панель

### Popup панель
- Появляется справа при клике на узел
- Опции: Теория, Видео, Задание
- Каждая опция открывает модальное окно с деталями курса

## 🔧 Технологии

- **React 18** - UI библиотека
- **Vite** - Сборщик и dev сервер
- **Tailwind CSS** - Утилитарный CSS фреймворк
- **React Router** - Роутинг
- **vis-network** - Визуализация графа знаний

## 📝 API интеграция

Приложение интегрировано с backend через `apiService.js`:

- Автоматическое определение demo режима
- JWT токены в localStorage
- Поддержка моков для разработки

### Demo режим

**Демо-режим включен по умолчанию!** 

Для отключения (если нужен реальный backend):
- Добавьте `?demo=false` к URL
- Или установите в localStorage: `localStorage.setItem('demo_mode', 'false')`

Для явного включения:
- Добавьте `?demo=true` к URL
- Или установите в localStorage: `localStorage.setItem('demo_mode', 'true')`

## 🎯 Роутинг

- `/` - Лендинг
- `/login` - Вход
- `/register` - Регистрация
- `/dashboard` - Главная панель (требует авторизации)
- `/profile` - Профиль (требует авторизации)
- `/community` - Сообщество (требует авторизации)
- `/faq` - FAQ
- `/about` - О проекте

## 🚧 Разработка

### Добавление нового компонента

1. Создайте файл в `src/components/`
2. Используйте Tailwind классы для стилизации
3. Импортируйте в нужную страницу

### Добавление новой страницы

1. Создайте файл в `src/pages/`
2. Добавьте роут в `src/App.jsx`
3. При необходимости добавьте защиту через `ProtectedRoute`

## 📦 Зависимости

Основные зависимости:
- `react` и `react-dom` - React библиотека
- `react-router-dom` - Роутинг
- `vis-network` - Граф знаний
- `tailwindcss` - CSS фреймворк
- `vite` - Сборщик

## 🐛 Известные проблемы

- vis-network загружается динамически, может быть задержка при первой загрузке
- В demo режиме все данные мокируются

## 📄 Лицензия

Часть проекта SuzyMiniCourse.

