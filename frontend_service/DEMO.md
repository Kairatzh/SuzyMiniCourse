# 🎮 Демо-режим FILL.AI

Демо-версия позволяет протестировать весь функционал приложения без необходимости запуска backend сервера.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd frontend_service
npm install
```

### 2. Запуск в демо-режиме

```bash
npm run dev
```

Приложение автоматически запустится в демо-режиме и будет доступно по адресу: `http://localhost:3000`

## ✨ Особенности демо-режима

### Автоматическая активация

Демо-режим **включен по умолчанию**. Это означает:

- ✅ Все данные мокируются
- ✅ Автоматический вход при первом запуске
- ✅ Не требуется backend сервер
- ✅ Все функции работают локально

### Автоматический вход

При первом открытии приложения:

1. Автоматически выполняется вход с учетными данными:
   - Email: `demo@fill.ai`
   - Пароль: `demo`

2. Вы сразу попадаете на Dashboard с графом знаний

3. Все данные уже загружены и готовы к использованию

### Доступные данные

В демо-режиме доступны:

- **6 готовых курсов**:
  - Основы машинного обучения
  - Python для начинающих
  - Дизайн интерфейсов
  - Тренировки
  - Русский язык
  - Английский язык

- **Граф знаний** с визуализацией связей между курсами

- **Генерация новых курсов** - работает с мокированными данными

### Баннер демо-режима

В верхней части экрана отображается баннер:

```
🎮 Демо-режим - Фронтенд работает без backend. Все данные мокируются.
```

Баннер можно закрыть, нажав на крестик.

## 🎯 Тестирование функционала

### 1. Граф знаний

- Откройте Dashboard
- Граф знаний отображается в центре экрана
- Кликните на любой узел графа
- Откроется popup панель справа с опциями: Теория, Видео, Задание

### 2. Генерация курса

1. Введите запрос в поисковую строку (например: "React")
2. Нажмите Enter или кнопку "+"
3. Подождите 2 секунды (имитация генерации)
4. Новый курс появится в графе знаний
5. Страница автоматически обновится для отображения нового курса

### 3. Просмотр курса

1. Кликните на узел графа
2. В popup панели выберите опцию (Теория/Видео/Задание)
3. Откроется модальное окно с деталями курса
4. Переключайтесь между вкладками

### 4. Профиль

- Перейдите в Профиль через Sidebar
- Просмотрите статистику
- Увидите список ваших курсов

### 5. Сообщество

- Перейдите в Сообщество
- Просмотрите посты
- Попробуйте лайкнуть пост

## 🔧 Отключение демо-режима

Если нужно использовать реальный backend:

### Вариант 1: Через URL

Добавьте параметр `?demo=false` к URL:
```
http://localhost:3000?demo=false
```

### Вариант 2: Через localStorage

Откройте консоль браузера и выполните:
```javascript
localStorage.setItem('demo_mode', 'false')
```

Затем обновите страницу.

### Вариант 3: Включить обратно

```javascript
localStorage.setItem('demo_mode', 'true')
// или
localStorage.removeItem('demo_mode') // вернется к значению по умолчанию (true)
```

## 📝 Примечания

- Все данные хранятся в памяти браузера
- При обновлении страницы сгенерированные курсы сохраняются
- Граф знаний автоматически обновляется при добавлении новых курсов
- Вход работает с любыми данными в демо-режиме

## 🐛 Известные ограничения

- Данные не сохраняются между сессиями (кроме токена авторизации)
- Граф обновляется только после перезагрузки страницы
- Некоторые функции могут работать не так, как с реальным backend

## 💡 Советы

1. **Быстрый вход**: В демо-режиме можно оставить поля входа пустыми - они автоматически заполнятся
2. **Генерация курсов**: Попробуйте разные темы для генерации
3. **Граф знаний**: Перетаскивайте узлы графа для лучшей визуализации
4. **Popup панель**: Кликайте на разные узлы, чтобы увидеть разные опции

## 🎉 Готово!

Теперь вы можете протестировать весь функционал FILL.AI без необходимости настройки backend!
