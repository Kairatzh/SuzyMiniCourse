# Fill AI — Платформа для генерации мини-курсов

**Fill AI** — это онлайн-платформа для автоматического создания мини-курсов с использованием ИИ-агентов. Система генерирует конспекты, тестовые задания, подборку видео и структурирует курсы в виде графов знаний.

##  Основные возможности

- **Генерация курсов через ИИ** — автоматическое создание конспектов, тестов и подборки видео
- **Многоагентная архитектура** — координатор определяет намерение и направляет к нужному агенту
- **Граф знаний** — структурирование курсов в Neo4j для удобной навигации
- **Telegram бот** — полнофункциональный бот для работы с платформой
- **RESTful API** — интеграция с внешними системами
- **Аутентификация** — безопасная система регистрации и входа

##  Архитектура проекта

Проект состоит из трех основных сервисов:

```
┌─────────────────────────────────────────────────────────┐
│                    Frontend Service                     │
│              (React + TailwindCSS)                      │
└─────────────────────────────────────────────────────────┘
                         ↕
┌─────────────────────────────────────────────────────────┐
│                   Backend Service                        │
│              (FastAPI + PostgreSQL)                      │
│  ┌──────────────┐              ┌──────────────┐         │
│  │   Users API  │              │ Courses API  │         │
│  └──────────────┘              └──────────────┘         │
└─────────────────────────────────────────────────────────┘
         ↕                              ↕
┌─────────────────────────────────────────────────────────┐
│                    AI Service                           │
│         (LangChain + LangGraph + LLM)                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │ Coordinator │→ │   Course     │  │ Chat Agent   │  │
│  │   Agent     │  │  Generation   │  │              │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### Компоненты

1. **AI Service** (`ai_service/`)
   - Координатор агент — определяет намерение (генерация курса или чат)
   - Агент генерации курсов — создает конспект, тесты, видео
   - Чат агент — отвечает на вопросы пользователей
   - LangGraph для маршрутизации между агентами

2. **Backend Service** (`backend_service/`)
   - FastAPI REST API
   - PostgreSQL для хранения данных
   - Neo4j для графа знаний
   - JWT аутентификация

3. **Telegram Bot** (`telegram_bot/`)
   - Полнофункциональный бот
   - Интеграция с Backend и AI сервисами
   - Регистрация, авторизация, генерация курсов

4. **Frontend Service** (`frontend_service/`)
   - React приложение
   - TailwindCSS для стилей
   - Визуализация графа знаний

##  Технологический стек

| Компонент | Технология |
|-----------|-----------|
| **Frontend** | React + TailwindCSS + Vite |
| **Backend** | FastAPI (Python) |
| **AI Service** | LangChain + LangGraph + OpenRouter |
| **Databases** | PostgreSQL + Neo4j |
| **Telegram Bot** | python-telegram-bot |
| **Authentication** | JWT (python-jose) |

##  Установка и запуск

>  **Подробная инструкция по установке**: см. [SETUP.md](SETUP.md)

### Предварительные требования

- Python 3.10+
- PostgreSQL 14+
- Neo4j 5+
- Node.js 18+ (для frontend)
- Telegram Bot Token (для бота)
- Docker и Docker Compose (опционально, для упрощенного запуска)

### Быстрый старт

1. **Клонирование репозитория**
```bash
git clone <repository-url>
cd SuzyMiniCourse
```

2. **Настройка переменных окружения**

Создайте `.env` файлы для каждого сервиса на основе `env.example`:

```bash
# Backend Service
cp backend_service/env.example backend_service/.env

# AI Service
cp ai_service/env.example ai_service/.env

# Telegram Bot
cp telegram_bot/env.example telegram_bot/.env
```

Заполните необходимые переменные (см. `env.example` файлы в каждой папке сервиса).

### 3. Установка зависимостей

#### Backend Service
```bash
cd backend_service
pip install -r requirements.txt
```

#### AI Service
```bash
cd ai_service
pip install -r requirements.txt
```

#### Telegram Bot
```bash
cd telegram_bot
pip install -r requirements.txt
```

#### Frontend Service
```bash
cd frontend_service
npm install
```

### 4. Настройка баз данных

#### PostgreSQL
```bash
# Создайте базу данных
createdb fill_ai

# Или используйте переменную DATABASE_URL в .env
```

#### Neo4j
```bash
# Запустите Neo4j (Docker)
docker run -d \
  --name neo4j \
  -p 7474:7474 -p 7687:7687 \
  -e NEO4J_AUTH=neo4j/password \
  neo4j:latest
```

### 5. Запуск сервисов

#### Backend Service
```bash
cd backend_service
python src/main.py
# Или
uvicorn src.main:app --host 0.0.0.0 --port 8001
```

#### AI Service
```bash
cd ai_service
python api/main.py
# Или
uvicorn api.main:app --host 0.0.0.0 --port 8000
```

#### Telegram Bot
```bash
cd telegram_bot
python src/main.py
```

#### Frontend Service
```bash
cd frontend_service
npm run dev
```

### 6. Использование Docker Compose (рекомендуется)

```bash
docker-compose up -d
```

Все сервисы будут запущены автоматически.

##  API Документация

После запуска сервисов, документация доступна по адресам:

- **Backend API**: http://localhost:8001/docs
- **AI Service API**: http://localhost:8000/docs

##  Telegram Bot

После запуска бота, используйте команды:

- `/start` - Начать работу
- `/help` - Справка по командам
- `/register` - Зарегистрироваться
- `/login` - Войти в систему
- `/generate` - Создать курс
- `/my_courses` - Мои курсы
- `/chat` - Чат с AI
- `/profile` - Профиль
- `/logout` - Выйти

##  Конфигурация

### Переменные окружения

Основные переменные для каждого сервиса описаны в соответствующих `.env.example` файлах.

### Важные настройки

- `OPENROUTER_API_KEY` - API ключ для OpenRouter (AI Service)
- `TELEGRAM_BOT_TOKEN` - Токен Telegram бота
- `DATABASE_URL` - URL подключения к PostgreSQL
- `NEO4J_URI`, `NEO4J_USER`, `NEO4J_PASSWORD` - Настройки Neo4j
- `SECRET_KEY` - Секретный ключ для JWT (Backend)

##  Структура проекта

```
FillAI/
├── ai_service/          # AI Service с агентами
│   ├── api/            # FastAPI приложение
│   ├── src/            # Исходный код
│   │   ├── agents/     # Агенты (координатор, генерация, чат)
│   │   ├── tools/      # Инструменты агентов
│   │   └── utils/      # Утилиты
│   └── config/         # Конфигурационные файлы
├── backend_service/     # Backend Service
│   ├── src/
│   │   ├── api/        # API endpoints
│   │   ├── database/   # Подключения к БД
│   │   ├── models/     # SQLAlchemy модели
│   │   └── services/   # Бизнес-логика
│   └── config/         # Конфигурация
├── telegram_bot/        # Telegram бот
│   └── src/
│       ├── handlers/   # Обработчики команд
│       ├── services/   # Клиенты API
│       └── utils/      # Утилиты
├── frontend_service/    # Frontend приложение
│   └── src/
├── docs/               # Документация
├── .gitignore
├── README.md
└── docker-compose.yml  # Docker Compose конфигурация
```

##  Разработка

### Запуск в режиме разработки

```bash
# Backend с автоперезагрузкой
uvicorn backend_service.src.main:app --reload --port 8001

# AI Service с автоперезагрузкой
uvicorn ai_service.api.main:app --reload --port 8000
```

### Тестирование

```bash
# Backend тесты
cd backend_service
pytest

# AI Service тесты
cd ai_service
pytest
```

##  Лицензия

MIT License - см. [LICENSE](LICENSE) файл

## 👥 Авторы

- kirtzh

##  Вклад в проект

Приветствуются любые вклады! Пожалуйста, создавайте issues и pull requests.

##  Поддержка

Для вопросов и поддержки создавайте issues в репозитории.

---

**Fill AI** — делаем образование доступным для всех! 
